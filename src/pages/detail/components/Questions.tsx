import { memo } from "react";

const Questions = () => {
  return (
    <div className="Questions">
      <h2 className="italic text-5xl">Questions......</h2>
    </div>
  );
};

export default memo(Questions);
